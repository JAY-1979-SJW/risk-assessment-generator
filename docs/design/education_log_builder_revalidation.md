# 교육일지 Excel Builder 재검증 보고서 — v1.1 패치 후

**검증일**: 2026-04-24  
**검증 스크립트**: `scripts/validate_education_log_builder.py`  
**대상 모듈**: `engine/output/education_log_builder.py` (v1.1 패치 4건 적용 후)

---

## 적용 패치 요약

| 패치 | 변경 내용 |
|------|---------|
| P1 | `_write_title`: 법적 근거 부제 행 추가 ("「산업안전보건법」 제29조에 따른 안전보건교육", 16pt) |
| P2 | `_write_subject_table`: 교육 내용 행 높이 20 → 30pt |
| P3 | `_write_attendee_table`: 수강자 행 높이 18 → 22pt |
| P4 | `_write_confirmation`: 서명란 행 높이 30 → 40pt |

---

## 검증 결과

| 항목 | 결과 |
|------|------|
| 총 검증 항목 | 25 |
| PASS | **25** |
| FAIL | 0 |
| **최종 판정** | **PASS** |

---

## 상세 결과

### 샘플 데이터 검증

```
  [PASS] xlsx bytes 정상 생성 — 8,064 bytes
  [PASS] openpyxl 재오픈 성공
  [PASS] 시트명 == '교육일지'
  [PASS] 제목 셀(A1) == '안전보건교육일지'
  [PASS] 필수 헤더 존재: '교육 내용'
  [PASS] 필수 헤더 존재: '수강자 명단'
  [PASS] 필수 헤더 존재: '교육 과목명'
  [PASS] 필수 헤더 존재: '교육 내용 요약'
  [PASS] 필수 헤더 존재: '성명'
  [PASS] 필수 헤더 존재: '직종(직위)'
  [PASS] 필수 헤더 존재: '서명 또는 날인'
  [PASS] 수강자 30행 존재
```

### 빈 form_data 검증

```
  [PASS] xlsx bytes 정상 생성 — 7,634 bytes
  [PASS] openpyxl 재오픈 성공
  [PASS] 시트명 == '교육일지'
  [PASS] 제목 셀(A1) == '안전보건교육일지'
  [PASS] 필수 헤더 존재: '교육 내용'
  [PASS] 필수 헤더 존재: '수강자 명단'
  [PASS] 필수 헤더 존재: '교육 과목명'
  [PASS] 필수 헤더 존재: '교육 내용 요약'
  [PASS] 필수 헤더 존재: '성명'
  [PASS] 필수 헤더 존재: '직종(직위)'
  [PASS] 필수 헤더 존재: '서명 또는 날인'
  [PASS] 수강자 30행 존재
```

### 공란 유지 검증

```
  [PASS] 모든 값 필드 공란 유지 — 비공란 비헤더 값: 없음
```

---

## v1 → v1.1 변경 비교

| 항목 | v1 | v1.1 | 변화 |
|------|-----|-------|-----|
| 총 행 수 (기본 3과목) | 45행 | 46행 (+부제 행) | +1 |
| xlsx 크기 (빈 form_data) | 7,553 bytes | 7,634 bytes | +81 bytes |
| 수강자 행 높이 | 18pt | 22pt | +4pt |
| 교육내용 행 높이 | 20pt | 30pt | +10pt |
| 서명란 행 높이 | 30pt | 40pt | +10pt |
| 법적 근거 부제 | 없음 | 있음 | 추가 |
| 검증 결과 | 25/25 PASS | 25/25 PASS | 유지 |

---

## 잔여 WARN (v1.1 이후)

모든 WARN 항목 패치 완료.

| 항목 | 상태 |
|------|------|
| 법적 근거 부제 누락 | **해소** (P1) |
| 교육내용 행 협소 | **해소** (P2) |
| 수강자 서명 공간 협소 | **해소** (P3) |
| 확인자 서명 공간 협소 | **해소** (P4) |
